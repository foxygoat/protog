syntax = "proto3";

enum Color {
  RED = 0;
  BLUE = 1;
  GREEN = 2;
}

message Coord {
  double x = 1;
  double y = 2;
}

message Circle {
  .Coord centre = 1;
  double radius = 2;
  // TODO: doesn't work yet: .Color color = 3;
}
